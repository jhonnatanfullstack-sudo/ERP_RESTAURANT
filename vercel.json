{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @restaurant-erp/frontend build",
  "outputDirectory": "apps/frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
